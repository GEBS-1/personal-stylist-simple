# Анализ ошибок CORS и механизма поиска товаров

## 🔍 Анализ ошибок из консоли

### 1. **Ошибки CORS (Cross-Origin Resource Sharing)**

**Проблема:** Браузер блокирует прямые запросы к Wildberries из-за политики безопасности CORS.

**Причины:**
- Wildberries не отправляет заголовок `Access-Control-Allow-Origin` в ответах
- Браузер запрещает cross-origin запросы без этого заголовка
- Добавленные нами заголовки для обхода блокировки (`pragma`, `dnt`, `x-requested-with`) не разрешены сервером

**Решение:** Требуется серверный прокси для обхода CORS.

### 2. **Как работает поиск товаров**

#### Этап 1: Генерация образа через Gemini
```typescript
// aiService.ts создает детальный JSON с предметами одежды
{
  "name": "Летний кэжуал для фигуры inverted-triangle",
  "items": [
    {
      "category": "Верх",
      "name": "Льняная рубашка",
      "description": "Свободная льняная рубашка с коротким рукавом...",
      "colors": ["Темно-синий"],
      "style": "Классический, кэжуал",
      "fit": "Свободный",
      "price": "3500"
    },
    // ... другие предметы
  ]
}
```

#### Этап 2: Определение категорий для поиска
```typescript
// wildberriesService.ts:getCategoriesByParams()
// На основе параметров определяются категории:
const categories = ['рубашка', 'брюки', 'футболка', 'джинсы', 'кроссовки', 'очки', 'ремень'];
```

#### Этап 3: Построение поисковых запросов
```typescript
// wildberriesService.ts:buildSearchQuery()
// Для каждой категории создается запрос:
"рубашка женская свободная повседневная"
"брюки женская свободная повседневная"
// и т.д.
```

#### Этап 4: Агрессивный поиск по 5 уровням
1. **Основной API** с обходом блокировки
2. **Альтернативные API** (каталог, мобильный)
3. **Веб-скрапинг** HTML страниц
4. **Локальный поиск** по ключевым словам
5. **Fallback** тестовые данные

### 3. **Почему поиск "невозможен"**

**Проблема пользователя:** "поиск через такой запрос вообще не возможен"

**Объяснение:**
1. **Слишком общие запросы:** "рубашка женская свободная повседневная" - слишком широкий поиск
2. **Отсутствие конкретных параметров:** нет размера, цвета, бренда
3. **CORS блокировка:** браузер не позволяет делать запросы к Wildberries
4. **Анти-бот защита:** Wildberries блокирует автоматические запросы

### 4. **Реальные решения**

#### Вариант 1: Серверный прокси
```javascript
// Создать backend API endpoint
app.get('/api/wildberries/search', async (req, res) => {
  const { query } = req.query;
  const response = await fetch(`https://search.wb.ru/exactmatch/ru/common/v4/search?query=${query}`);
  const data = await response.json();
  res.json(data);
});
```

#### Вариант 2: Улучшенные поисковые запросы
```typescript
// Использовать конкретные данные из сгенерированного образа
const searchQuery = `${item.name} ${item.colors.join(' ')} ${item.style}`;
// Пример: "Льняная рубашка Темно-синий Классический кэжуал"
```

#### Вариант 3: Использование реального API
- Получить официальный API ключ от Wildberries
- Использовать их SDK или документацию

### 5. **Текущее состояние**

- ✅ Gemini генерирует образы корректно
- ✅ JSON парсинг работает
- ❌ CORS блокирует реальные запросы к Wildberries
- ❌ Агрессивный парсинг не работает из-за браузерных ограничений
- ✅ Fallback данные показываются корректно

### 6. **Рекомендации**

1. **Немедленно:** Использовать fallback данные для демонстрации
2. **Краткосрочно:** Создать простой backend прокси
3. **Долгосрочно:** Интегрировать с официальным API Wildberries
4. **Альтернатива:** Использовать другие маркетплейсы с открытым API 